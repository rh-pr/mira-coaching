export type OffersCardType = {
    img: string,
    name: string,
    offers: string[]
}

export type ReviewType = {
    id: string,
    note: number;
    fname: string, 
    lname: string,
    content: string
}